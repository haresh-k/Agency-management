Add front end using Angular
Move docker image building to the gradle script - can avoid multiple execution cmds
Good to have: Loggers, Swagger docs
Add postman collection for the APIs to repo
Setup TODO as part of gradle tasks
Separate service layer out from controllers
Add build files to gitignore